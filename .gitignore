# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# OS / editor
.DS_Store

# Tooling
.playwright-mcp/

# Experiment harness output and debug artifacts (not part of the package)
experiments/
rsgrove_partitions_debug.csv

# Datasets: only the checked-in sample dataset + catalog index are tracked.
# Everything else under datasets/ (uploads, derived results, large data) is local.
/datasets/*
!/datasets/OSM2015_33/
!/datasets/_catalog/
/datasets/_uploads/
